// Basic initialization
window.addEventListener('DOMContentLoaded', (event) => {
    const portalElement = document.getElementById('matthew-parish-portal');
    if (portalElement) {
        portalElement.innerHTML = 'Parish Portal is initializing...';
    }
});
